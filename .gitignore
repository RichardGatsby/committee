# caches
.cache/
.pytest_cache/
__pycache__/
*.pyc

# generated reports. Output is derived from coefficients.json and goes stale
# the moment the model is refitted, so it is never committed.
*.csv
*.report.md
scan.json

# the rendered site. Built by the workflow, never committed.
_site/

# macOS, editors, envs
.DS_Store
.venv/
venv/
.idea/
.vscode/
tier-history.txt

# git worktrees created for parallel work
.claude/worktrees/
